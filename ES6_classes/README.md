# ES6_classes

## 0. You used to attend a place like this at some point

### Description
This project introduces ES6 classes in JavaScript. The goal is to implement a basic class with a constructor that accepts and stores a parameter.

### Files
- `0-classroom.js`: Contains the `ClassRoom` class implementation.

### Tasks
1. **ClassRoom**: Create a class that accepts `maxStudentsSize` as a parameter and assigns it to `_maxStudentsSize`.

### Usage
To test the class, run:
```bash
npm run dev 0-main.js
